// Métodos de Cadenas
concat() junta dos o más cadenas y retorna una nueva

startsWith() si una cadena comieza con los caracteres de otra cadena, devuelve true, sino false
endsWith() si una cadena termina con los caracteres de otra cadena, devuelve true, sino false
includes() si una cadena puede encontrarse dentro de otra cadena, devuelve true, sino false
indexOf() devuelve el indice del primer caracter de la cadena, si no existe, devuelve -1
lastIndexOf() devuelve el ultimo indice del primer caracter de la cadena, si no existe, devuelve -1

padStart() rellenar cadena al principio con los caracteres deseados
padEnd() rellenar cadena al final con los caracteres deseados
repeat() devuelve la misma cadena pero repetida la cantidad que le indiquemos

split() divide la cadena como le pidamos
substring() nos retorna un pedazo de la cadena que seleccionemos
toLowerCase() convierte una cadena a minuscula
toUpperCase() convierte en mayusculas
toString() metodo que devuelve una cadena que representa al objeto especificiado
trim() elimina los espacios en blanco al principio y al final de una cadena
trimEnd() elimina los espacios en blanco al final de una cadena
trimStart() elimina espacios en blanco al comienzo de una cadena
valueOf() retorna el valor primitivo de un objeto String